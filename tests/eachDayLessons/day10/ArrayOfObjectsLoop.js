const products = [
    {name: "Laptop", price: 50000},
    {name: "Bag", price: 1000}
]

for (let index = 0; index < products.length; index++) {
    const element = products[index];
    console.log(element);
}